Create the fastest possible single-file Java program named hashprime.java that takes one integer N from the command line. Generate all prime numbers from 2 to N using a Sieve of Eratosthenes, hash them and print the hash. a fast hash can be calculated from prime bytes. consider an infinite list of primes. prime bytes : prime digits \n prime digits \n ... until range ends. a prime is followed by a single newline byte ('\n'). Compute the SHA-256 digest over the prime bytes and print to stdout, ONLY the hash (as a lowercase hex string) to stdout. The digest over the prime bytes for N=11 is exactly 563d8e0603dcc07d784135d99fd81ff6bf98495e898ec1f52e2e7605320cf6dc, and N=12 is identical (12 is not prime), so both must produce that same hash.


VALIDATION CONTRACT (the SHA-256 is computed over the  prime bytes; stdout shows ONLY the hash):
  Command:              java hashprime 11
  Prime bytes :      2\n3\n5\n7\n11\n
  Expected SHA-256 (stdout):     563d8e0603dcc07d784135d99fd81ff6bf98495e898ec1f52e2e7605320cf6dc

  Command:              java hashprime 12
  Prime bytes :      2\n3\n5\n7\n11\n   (12 is not prime, so identical to N=11)
  Expected SHA-256 (stdout):     563d8e0603dcc07d784135d99fd81ff6bf98495e898ec1f52e2e7605320cf6dc

  Command:              java hashprime 1000000
  Expected SHA-256 (stdout):     4883963dd4510a29d6df2ffe4dd11e4e1a910e815c7810b200c77b3357f22a28

  Command:              java hashprime 10000000
  Expected SHA-256 (stdout):     36d6197802bc3b635b43b31cd6a2583f7cf8f5badff7992f3693c5102beefd14

Use public class hashprime (lowercase) to match the name hashfile.java.
Respond with a JSON tool call to use a tool, or plain text to communicate.

Your program must produce the prime bytes (and therefore the matching digest) at ALL four scales. The same algorithm must be correct from N=2 through N=10000000.range could be large.

